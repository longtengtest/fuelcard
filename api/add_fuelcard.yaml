name: 添加加油卡
request:
  method: POST
  url: /gasStation/process
  json:
    dataSourceId: ${ENV(DATA_SOURCE_ID)}
    methodId: 00A
    CardInfo:
      cardNumber: $card_number

# FOR DEBUG
base_url: ${ENV(BASE_URL)}
variables:
  card_number: hzc_00001